#lang racket
(require "set-covering")
(require "base")
;; id file
;; vnd estruturas
;; vnd vizinhos
;; taxa-mutacao (em porcentagem)
;; tamanho da população
;; qtdade de iguais para parada
;; tempo limite
(define melhor-resultado-1 (set-covering-algorithm 1 2 20 15 150 70 350))